min_version: 1.11.0

pre-commit:
  parallel: false
  commands:
    fmt:
      run: cargo fmt
      glob: "*.rs"
      stage_fixed: true
      priority: 1
    clippy:
      run: cargo clippy --all-targets --all-features -- -D warnings
      glob: "*.rs"
      priority: 2

pre-push:
  parallel: false
  commands:
    clippy:
      run: cargo clippy --all-targets --all-features -- -D warnings
      priority: 1
    test:
      run: env -u NO_COLOR TERM=xterm-256color COLORTERM=truecolor CLAUDE_TRUECOLOR=1 CARGO_TERM_COLOR=always cargo test --all-features
      priority: 2
